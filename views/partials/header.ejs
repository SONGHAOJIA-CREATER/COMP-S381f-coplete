<!DOCTYPE html>
<html lang="<%= lang === 'en' ? 'en' : 'zh-Hans' %>">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>
      <%= typeof title !== 'undefined'
        ? `${title} · ${tr('校园二手集市', 'Campus Marketplace')}`
        : tr('校园二手集市', 'Campus Marketplace') %>
    </title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body
    data-confirm-message="<%= tr('确定要删除该物品吗？删除后无法恢复。', 'Are you sure you want to delete this item? This action cannot be undone.') %>"
  >
    <nav class="navbar navbar-expand-lg bg-body-tertiary shadow-sm">
      <div class="container">
        <a class="navbar-brand fw-bold" href="/"><%= tr('校园二手集市', 'Campus Marketplace') %></a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navMenu"
          aria-controls="navMenu"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navMenu">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <% if (currentUser) { %>
            <li class="nav-item">
              <a class="nav-link <%= activeNav === 'items' ? 'active' : '' %>" href="/items"><%= tr('物品广场', 'Marketplace') %></a>
            </li>
            <li class="nav-item">
              <a class="nav-link <%= activeNav === 'new' ? 'active' : '' %>" href="/items/new"><%= tr('发布物品', 'Post Item') %></a>
            </li>
            <% } %>
          </ul>
          <div class="d-flex align-items-center gap-3">
            <form action="/lang/<%= lang === 'zh' ? 'en' : 'zh' %>" method="POST" class="mb-0">
              <button class="btn btn-outline-secondary btn-sm" type="submit">
                <%= lang === 'zh' ? 'English' : '中文' %>
              </button>
            </form>
            <% if (currentUser) { %>
            <span class="text-muted small">
              <%= tr('你好，', 'Hello, ') %><%= currentUser.username %>
            </span>
            <form action="/logout" method="POST">
              <button class="btn btn-outline-danger btn-sm"><%= tr('退出登录', 'Log Out') %></button>
            </form>
            <% } else { %>
            <a href="/login" class="btn btn-outline-primary btn-sm"><%= tr('登录', 'Log In') %></a>
            <a href="/register" class="btn btn-primary btn-sm"><%= tr('注册', 'Sign Up') %></a>
            <% } %>
          </div>
        </div>
      </div>
    </nav>

    <main class="container py-4">
      <% if (flash) { %>
      <div class="alert alert-<%= flash.type %> alert-dismissible fade show" role="alert">
        <%= flash.message %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
      <% } %>

